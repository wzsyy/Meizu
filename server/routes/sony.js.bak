const express=require("express");
const pool=require("./pool");//接收pool
var router=express.Router();


/*
router.get("/xxx/:aa",(req,res)=>{
    var aa=req.params.aa;
    var sql="...";
    pool.query(sql,aa,(err,result)=>{
        if(err)throw err;
        res.send(result);
    });
});

router.post("/xx",(req,res)=>{
    var bb=req.body.bb;  
}) 
*/

module.exports=router;